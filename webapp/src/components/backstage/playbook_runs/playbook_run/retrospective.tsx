import React from 'react';
import styled from 'styled-components';

const Retrospective = () => {
    return <Container>{'Retrospective'}</Container>;
};

export default Retrospective;

const Container = styled.div`
    display: flex;
    flex-direction: row;
`;
