import React from 'react';
import styled from 'styled-components';

const StatusUpdate = () => {
    return <Container>{'Status update'}</Container>;
};

export default StatusUpdate;

const Container = styled.div`
    display: flex;
    flex-direction: row;
`;